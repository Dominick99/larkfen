# Claude Code guidance

Treat `AGENTS.md` as the canonical repository guide. Read it before making
changes. Project-local task workflows live under `.agents/skills/`; use the
matching `SKILL.md` when a request fits its description.
