Use `AGENTS.md` as the canonical repository map, architecture constraints, and
verification guide. Task-specific workflows are stored as Agent Skills under
`.agents/skills/` and should be loaded only when relevant.
